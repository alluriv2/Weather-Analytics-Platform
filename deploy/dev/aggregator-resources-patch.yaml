apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-aggregator
spec:
  template:
    spec:
      containers:
        - name: aggregator
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
